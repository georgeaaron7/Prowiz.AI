go to https://developer.adobe.com/console, sign-in and then create a project> add api> PDF services api. 
this should return a json file, then export your clinet keys into your env. using EXPORT and then run it normally.
just add a output directory and a resources directory with the pdf or change the path in the code.
